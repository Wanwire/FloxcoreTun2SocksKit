diff --git a/thirdparty/hev-socks5-tunnel-2.7.4/src/misc/hev-exec.c b/thirdparty/hev-socks5-tunnel-2.7.4/src/misc/hev-exec.c
index 287b60f..eb1c9d9 100644
--- a/thirdparty/hev-socks5-tunnel-2.7.4/src/misc/hev-exec.c
+++ b/thirdparty/hev-socks5-tunnel-2.7.4/src/misc/hev-exec.c
@@ -28,15 +28,18 @@ hev_exec_run (const char *script_path, const char *tun_name, int wait)
 {
 }
 #else
+#if 0
 static void
 signal_handler (int signum)
 {
     waitpid (-1, NULL, WNOHANG);
 }
+#endif
 
 void
 hev_exec_run (const char *script_path, const char *tun_name, int wait)
 {
+#if 0
     pid_t pid;
 
     signal (SIGCHLD, signal_handler);
@@ -52,8 +55,10 @@ hev_exec_run (const char *script_path, const char *tun_name, int wait)
     }
 
     execl (script_path, script_path, tun_name, NULL);
-
+#else
+    (void)wait;
     LOG_E ("exec %s %s", script_path, tun_name);
     exit (-1);
+#endif
 }
 #endif
diff --git a/thirdparty/hev-socks5-tunnel-2.7.4/src/misc/hev-utils.c b/thirdparty/hev-socks5-tunnel-2.7.4/src/misc/hev-utils.c
index 6c2316a..b44a0de 100644
--- a/thirdparty/hev-socks5-tunnel-2.7.4/src/misc/hev-utils.c
+++ b/thirdparty/hev-socks5-tunnel-2.7.4/src/misc/hev-utils.c
@@ -26,6 +26,7 @@
 void
 run_as_daemon (const char *pid_file)
 {
+#if 0
     FILE *fp;
 
     fp = fopen (pid_file, "w+");
@@ -45,6 +46,9 @@ run_as_daemon (const char *pid_file)
 
     fprintf (fp, "%u\n", getpid ());
     fclose (fp);
+#else
+    (void)pid_file;
+#endif
 }
 
 int
diff --git a/thirdparty/hev-socks5-tunnel-2.7.4/third-part/lwip/src/ports/unix/netif/sio.c b/thirdparty/hev-socks5-tunnel-2.7.4/third-part/lwip/src/ports/unix/netif/sio.c
index 1a59945..b66fd35 100644
--- a/thirdparty/hev-socks5-tunnel-2.7.4/third-part/lwip/src/ports/unix/netif/sio.c
+++ b/thirdparty/hev-socks5-tunnel-2.7.4/third-part/lwip/src/ports/unix/netif/sio.c
@@ -72,7 +72,7 @@
 #endif
 
 #ifndef LWIP_HAVE_SLIPIF
-#define LWIP_HAVE_SLIPIF 1
+#define LWIP_HAVE_SLIPIF 0
 #endif
 
 /** Maximum packet size that is received by this netif */
